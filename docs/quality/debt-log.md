# Debt Log

Track known risks here instead of relying on chat history.

| Date | Area | Debt | Risk | Next Step |
| --- | --- | --- | --- | --- |
| 2026-05-14 | Auto sync | Primary auto-sync still uses a UI readiness poll, backed by process-lifetime watchers and a two-minute metadata fallback sweep. | Auto-sync depends on the Tauri webview remaining alive; a fully backend-owned scheduler is still absent. | Move readiness scheduling into Rust if headless/background operation becomes a product requirement. |
| 2026-05-14 | Retry | Retryable transfer failures are surfaced, but durable retry queue/replay is still limited. | Network/VPN changes can require another user action. | Add persistent outbound queue with backoff after minimal flow stabilizes. |
| 2026-05-18 | Packaging | The GitHub macOS channel uses a free ad-hoc signature but intentionally has no Developer ID identity or notarization. | Gatekeeper still requires manual approval; managed Macs may reject the app; local-network permission identity may remain less reliable across upgrades. | Verify the ad-hoc signature and checksum for every DMG. If installed-app `EHOSTUNREACH` persists while Terminal works, retain Developer ID signing/notarization as the reliable future option. |
| 2026-07-18 | Native safety validation | Windows `ReplaceFileW` retry/Junction checks and macOS APFS replacement/symlink flows have CI coverage configured but have not been observed on the required two real devices in this worktree. | Platform filesystem and packaging behavior can still differ from unit tests or cross-checks. | Keep release blocked until the documented NTFS/APFS dual-device matrix is signed off. |
| 2026-07-18 | Handle-relative path mutation | Current mutation paths reject symlink/reparse components and revalidate immediately before mutation, but the complete Unix `openat` / Windows directory-handle refactor is not yet finished for every history and conflict operation. | A highly privileged local attacker racing directory replacement may retain a narrow TOCTOU window. | Finish `TaskRootHandle` handle-relative operations and native race tests before treating hostile-local-user containment as complete. |
| 2026-07-18 | Peer connection state | Durable peer-scoped disconnect intent and revision ordering are implemented, but loss/retry/restart behavior has not yet been observed on the required Windows/macOS pair. | Platform startup timing or firewall behavior could delay state publication despite unit/UI coverage. | Keep the connection-state release gate open until the documented dual-device restart, restore, and network-loss matrix passes. |
